services:
  groupdocs-signature-mcp:
    image: ghcr.io/groupdocs-signature/signature-net-mcp:latest
    stdin_open: true
    tty: false
    environment:
      GROUPDOCS_MCP_STORAGE_PATH: /data
    volumes:
      - ./documents:/data
